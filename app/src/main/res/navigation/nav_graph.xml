<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/blankFragment">

    <fragment
        android:id="@+id/blankFragment"
        android:name="com.example.appclima.ui.BlankFragment"
        android:label="login"
        tools:layout="@layout/login" >
        <action
            android:id="@+id/action_blankFragment_to_notaActivity"
            app:destination="@id/notaActivity" />
    </fragment>
    <activity
        android:id="@+id/notaActivity"
        android:name="com.example.appclima.NotaActivity"
        android:label="NotaActivity" />
</navigation>